# ngrok (required for "npm start"). Get a free token: https://dashboard.ngrok.com/get-started/your-authtoken
# NGROK_AUTHTOKEN=your_ngrok_token_here

# Optional: app URL for one-click share link. When set, "npm start" prints a link that auto-connects (no paste).
REMOTE_LIMELIGHT_APP_URL=https://limelight-remote.vercel.app

# Optional: when testing without a robot, point bridge at fake Limelight (run "npm run test:fake" in another terminal).
# LIMELIGHT_ORIGIN=http://127.0.0.1:5800

# Optional: append each controller JSON line to this file (for testing or feeding robot code).
# BRIDGE_CONTROLLER_LOG=controller.log
